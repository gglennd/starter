# Logging
# Minimum log level for the pino transport (trace | debug | info | warn | error | fatal). Defaults to "info".
LOG_LEVEL=info

# Client
# Absolute origin a browser uses to reach the app (client env, exposed as VITE_*).
VITE_APP_ORIGIN=http://localhost:5173