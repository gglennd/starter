# Logging
# Minimum log level for both transports (trace | debug | info | warn | error | fatal). Defaults to "info".
LOG_LEVEL=info

# When unset, the HTTP transport is disabled and only console logging is active.
LOG_HTTP_URL=

# Optional bearer token sent as `Authorization: Bearer <key>`.
LOG_API_KEY=